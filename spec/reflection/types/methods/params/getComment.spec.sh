# add a [2] third lookup table to TYPE for method parameter comments, each in its own index, but nothing happens if T_COMMENTS != enabled so it's safe - include 'returns'
#
# Method.param
# Method.__T__RETURNS__
#
# plus we'll work on making snapshots super easy