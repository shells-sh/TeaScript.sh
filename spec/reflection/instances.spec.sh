# e.g. calling methods on instances and getting/setting fields on instances
#
# This will include some type checking -vs- the objects layer which does no checking, its only responsible for memory/storage